{
  "name": "@team-hub/web",
  "version": "0.1.0",
  "private": true,
  "description": "Team Hub frontend — Next.js 16 (App Router) + Tailwind 4 + Zustand + TanStack Query.",
  "scripts": {
    "dev": "next dev --turbopack --port 3000",
    "build": "next build",
    "start": "next start --port 3000",
    "lint": "next lint",
    "test": "echo 'web tests TBD' && exit 0"
  },
  "dependencies": {
    "@team-hub/schemas": "workspace:*"
  },
  "devDependencies": {
    "@team-hub/eslint-config": "workspace:*"
  }
}
